import React from 'react';
import NavigationPanel from '../Components/Navigation/NavigationPanel';

const ReserveCar = () => (
  <div style={{ display: 'flex' }}>
    <NavigationPanel />
    <h1 className="text-center">ReserveCar</h1>
  </div>
);

export default ReserveCar;
